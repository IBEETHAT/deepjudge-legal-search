# Approval Criteria

Approve when:
- tests pass for the changed behavior
- no API secrets are exposed to browser-side code or committed files
- the Python client and mobile web app entrypoints remain functional
- documentation reflects any user-visible runtime or install changes
