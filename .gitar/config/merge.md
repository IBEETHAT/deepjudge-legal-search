# Merge Criteria

Before merge:
- confirm the working tree changes are intentional and validated
- ensure any new runtime behavior has matching tests or a clear reason it cannot
- ensure iPhone/mobile app changes preserve installability assets and routes
- ensure packaging or entrypoint changes still allow `python -m deepjudge_client` to run
