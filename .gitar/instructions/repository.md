# Repository Instructions

- This repository is a Python package for the DeepJudge legal search client plus an installable mobile web app.
- Use absolute repository paths in all file references.
- Favor small, targeted changes and keep tests in `tests/` updated with behavior changes.
- Validate changes with the existing unittest-based test suite when code or runtime behavior changes.
